# Production Environment Configuration

# HTTP Configuration
http.host=0.0.0.0
http.port=8081

# File System Configuration
file.working.dir=/opt/files-to-idp-to-sap
file.input.dir=/opt/files-to-idp-to-sap/input
file.archive.dir=/opt/files-to-idp-to-sap/archive
file.polling.frequency=60

# IDP Configuration
idp.host=idp.company.com
idp.port=443
idp.protocol=HTTPS
idp.auth.path=/oauth/token
idp.data.path=/api/v1/data

# SAP Configuration
sap.host=sap-prod.company.com
sap.system.number=00
sap.client=300
sap.username=PROD_USER
sap.language=EN

# SFTP Configuration (if needed)
sftp.host=sftp.company.com
sftp.port=22
sftp.username=prod_sftp_user